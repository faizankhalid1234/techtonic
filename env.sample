# Frontend (Next.js) — API runs in ./backend (npm run dev --prefix backend)
JWT_SECRET=must_match_backend_jwt_secret
BACKEND_URL=https://backend-techtonic.vercel.app

# Optional: WhatsApp order button in header (digits only, country code, no +)
# Example Pakistan: 923001234567
NEXT_PUBLIC_WHATSAPP_PHONE=
